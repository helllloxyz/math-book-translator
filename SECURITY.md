# Security Policy

## 部署边界

Math Book Translator 当前是单实例、可信用户工具，不包含登录、多租户隔离、角色权限或请求配额。默认生产脚本只监听 `127.0.0.1`。

不要把应用端口直接暴露到不受信任的公网。公网访问的身份认证、TLS、速率限制和网络边界应由部署环境或用户选择的代理软件负责。

## 敏感数据

- 模型 API Key 保存在 `backend/config/llm_credentials.json` 或 `LLM_CREDENTIALS_FILE` 指向的文件中。
- `.env`、数据库、图书存储、日志和备份可能包含私人内容。
- 图书正文、用户问题、Quiz 回答和笔记可能被发送到所配置的模型服务商。
- 密钥文件为明文；请使用最小文件权限、独立系统账户和受保护的备份。

仓库的 `.gitignore` 会排除默认敏感文件，但不能代替提交前检查。发布前应扫描完整 Git 历史，而不仅是当前工作树。

## 漏洞报告

请不要在公开 issue 中提交可利用细节、真实 API Key、个人数据或受版权保护的书籍内容。维护者启用托管平台的私有安全报告功能后，应优先通过私有安全报告提交，并包含：

- 受影响版本或 commit。
- 复现所需的最小步骤。
- 实际影响和攻击前提。
- 已验证的缓解方式（如有）。

维护者应在公开仓库前补充明确的安全联系渠道和响应时限。

